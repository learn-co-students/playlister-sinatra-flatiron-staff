<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>New Songs</title>
  </head>
  <body>
    <form id="new_song" action="/songs" method="post">
      <label>New Song Name:</label>
      <input type="text" id="Name" name="Name" value="Name">
      <br><br>

      <label>Select an artist for that song:</label>
      <%Artist.all.each do |a|%>
        <input type="checkbox" name="artists[]" value="<%=a.id%>" id="<%=a.name%>"><%=a.id%></input>
        <br>
      <%end%>
      <label>Or, create an artist of that song:</label>
      <input type="text" id="Artist Name" name="artist" value="Artist Name">
      <br><br>

      <label>Select a genre for that song:</label>
      <%Genre.all.each do |g|%>
        <input type="checkbox" name="genres[]" value="<%=g.id%>" id="<%=g.name%>"><%=g.id%></input>
        <br>
      <%end%>


      <input type="submit" id="Submit" name="submit" value="Create">

    </form>
  </body>
</html>
